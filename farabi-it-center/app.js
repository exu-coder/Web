const form=document.getElementById('applicationForm');const status=document.getElementById('status');form.addEventListener('submit',async e=>{e.preventDefault();const data=Object.fromEntries(new FormData(form));status.textContent='আবেদন জমা দেওয়া হচ্ছে…';const url=window.FARABI_SUPABASE_URL,key=window.FARABI_SUPABASE_ANON_KEY;try{if(!url||!key){localStorage.setItem('farabi_pending_application',JSON.stringify({...data,created_at:new Date().toISOString()}));status.textContent='ডেমো মোডে আবেদন গ্রহণ করা হয়েছে। Supabase সংযোগ করলে এটি ডাটাবেসে যাবে।';form.reset();return}const r=await fetch(`${url}/rest/v1/applications`,{method:'POST',headers:{'Content-Type':'application/json',apikey:key,Authorization:`Bearer ${key}`,Prefer:'return=minimal'},body:JSON.stringify(data)});if(!r.ok)throw Error(await r.text());status.textContent='আবেদন সফলভাবে জমা হয়েছে। ধন্যবাদ!';form.reset()}catch(err){console.error(err);status.textContent='আবেদন জমা দিতে সমস্যা হয়েছে। পরে আবার চেষ্টা করুন।'}});